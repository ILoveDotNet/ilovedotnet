<code class=@($"[ p-0.5 ] [ border border-black ] [ dark:border-white ] {CssClasses}")>@ChildContent</code>

@code {
    [Parameter, EditorRequired] public RenderFragment ChildContent { get; set; } = default!;
    [Parameter] public string? CssClasses { get; set; } = default!;
}