Copilot AI Refactoring

Inside TDDDemoComponents, look for every razor pages inside all folders (meaning razor components having @page attribute inside it) and then in those pages find the image tag and in each image tag if the image file name contains space then replace it with - and lowercase the filename and update the same in alt tag in image and also rename the corresponding image file inside wwwroot/image/blogs/tdd/ subfolders only.. do it one by one so that you dont end up in rate limit. After that build and stage and do a no verify git commit and push to remove as follows git commit -m "remove spce in image files and rename them with hyphen" --no-verify