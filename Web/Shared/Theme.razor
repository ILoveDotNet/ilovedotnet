@inherits ThemeBase

<section id="displaymode">
    <div class="dropdown @(MenuCollapsed ? null : "is-active")" @onclick="ToggleMenu" @onfocusout="FocusOutHandler">
        <div class="dropdown-trigger">
            <button class="button [ flex items-center ]" aria-haspopup="true" aria-controls="displaymode-menu">
                <img src="@($"icons/{Icon}.svg")" alt="Theme" height="25" width="25" />
            </button>
        </div>
        <div class="dropdown-menu" id="displaymode-menu" role="menu">
            <div class="dropdown-content [ dark:bg-black ]">
                <button type="button" class="dropdown-item [ flex items-center ] [ dark:hover:bg-gray-600 ]" @onmousedown="@(_ => SetTheme(DisplayMode.Light))">
                    <img src="icons/brightness-up.svg" alt="Light" height="25" width="25" onload="SVGInject(this)" />
                    <p class="[ space-x-1 ] [ dark:text-white ]">Light</p>
                </button>
                <button type="button" class="dropdown-item [ flex items-center ] [ dark:hover:bg-gray-600 ]" @onmousedown="@(_ => SetTheme(DisplayMode.Dark))">
                    <img src="icons/moon.svg" alt="Dark" height="20" width="20" onload="SVGInject(this)" />
                    <p class="[ space-x-1 ] [ dark:text-white ]">Dark</p>
                </button>
                <button type="button" class="dropdown-item [ flex items-center ] [ dark:hover:bg-gray-600 ]" @onmousedown="@(_ => SetTheme(DisplayMode.System))">
                    <img src="icons/device-desktop.svg" alt="System" height="20" width="20" onload="SVGInject(this)" />
                    <p class="[ space-x-1 ] [ dark:text-white ]">System</p>
                </button>
            </div>
        </div>
    </div>
</section>