@inherits LayoutComponentBase
@inject AppState AppState
@implements IDisposable

<Header></Header>
<main id="main" class="[ @(AppState.ShowNavigation ? "[ pb-14 md:pb-0 md:pl-16 ]" : "[ transition-all delay-75 duration-150 ease-in-out ]") ]">
    <section class="[ font-bold ] [ p-5 ] [ flex items-center justify-center ]">
        <a href="https://whatsapp.com/channel/0029VaAGMV2LtOj5S5MHd23h" target="_blank">
            <span>👉🏼 Click here to Join I ❤️ .NET </span>
            <svg xmlns="http://www.w3.org/2000/svg" class="[ icon icon-tabler icon-tabler-brand-whatsapp ] [ fill-green-500 ] [ inline ]" width="30" height="30" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round">
                <path stroke="none" d="M0 0h24v24H0z" fill="none"></path>
                <path d="M3 21l1.65 -3.8a9 9 0 1 1 3.4 2.9l-5.05 .9"></path>
                <path d="M9 10a.5 .5 0 0 0 1 0v-1a.5 .5 0 0 0 -1 0v1a5 5 0 0 0 5 5h1a.5 .5 0 0 0 0 -1h-1a.5 .5 0 0 0 0 1"></path>
            </svg>
            <span>WhatsApp Channel to get 🔔 notified about new articles and other updates.</span>
        </a>
    </section>
    @Body
</main>
@if (AppState.ShowNavigation)
{
    <Navigation></Navigation>
}
<BackToTop></BackToTop>

@code {
    protected override void OnInitialized()
    {
        AppState.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        AppState.OnChange -= StateHasChanged;
    }
}